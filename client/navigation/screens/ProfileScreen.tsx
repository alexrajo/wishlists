import { View, Text } from "react-native";

const ProfileScreen = ({navigation}: any) => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
}

export default ProfileScreen;