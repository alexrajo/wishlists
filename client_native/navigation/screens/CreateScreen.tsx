import { View, Text } from "react-native";

const CreateScreen = ({navigation}: any) => {
    return (
        <View>
            <Text>Create</Text>
        </View>
    );
}

export default CreateScreen;