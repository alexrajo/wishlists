FROM node:16-alpine
WORKDIR /app
COPY ./server .
RUN ls
RUN npm install
EXPOSE 3001
ARG DATABASE_CONNECTION_STRING
ENV DATABASE_URL=${DATABASE_CONNECTION_STRING}
ENV PORT=3001
RUN npx prisma generate
CMD ["npm", "start"]
